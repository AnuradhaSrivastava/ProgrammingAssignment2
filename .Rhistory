makeVector<-function(X=numeric()) {
m<-NULL
set<-function(y) {
x<<-y
m<<-NULL
}
get<-function() x
setmean<-function(mean) m<<- mean
getmean<-function() m
list(set=setm, get=get,
setmean=setmean
getmean=getmean)
}
makeVector<-function(X=numeric()) {
m<-NULL
set<-function(y) {
x<<-y
m<<-NULL
}
get<-function() x
setmean<-function(mean) m<<- mean
getmean<-function() m
list(set=setm, get=get,
setmean=setmean,
getmean=getmean)
}
makeVector
makeVector(x)
makeVector(5)
makeVector
makeVector<-function(x=numeric()) {
m<-NULL
set<-function(y) {
x<<-y
m<<-NULL
}
get<-function() x
setmean<-function(mean) m<<- mean
getmean<-function() m
list(set=setm, get=get,
setmean=setmean
getmean=getmean)
}
makeVector<-function(x=numeric()) {
m<-NULL
set<-function(y) {
x<<-y
m<<-NULL
}
get<-function() x
setmean<-function(mean) m<<- mean
getmean<-function() m
list(set=setm, get=get,
setmean=setmean,
getmean=getmean)
}
makeVector(x)
makeVector(5)
makeVector 5
makeVector
a<-makeVector(c(1,2,3,4))
Q
f
